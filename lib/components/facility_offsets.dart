import 'package:flutter/material.dart';
import 'package:Dubeogi/save/save.dart';
// 편의시설들 버튼 누르면 뜨는 아이콘의 위치들.

List<Widget> vendingPositionedList({required double scale, required double scale_offset}) {
  List<Widget> widgets = [
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2456 * scale_offset - 50 / scale,
        top: 2808 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2602 * scale_offset - 50 / scale,
        top: 2701 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2346 * scale_offset - 50 / scale,
        top: 3037 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2396 * scale_offset - 50 / scale,
        top: 3140 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2178 * scale_offset - 50 / scale,
        top: 3099 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2188 * scale_offset - 50 / scale,
        top: 3030 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1923 * scale_offset - 50 / scale,
        top: 2930 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1656 * scale_offset - 50 / scale,
        top: 2869 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1738 * scale_offset - 50 / scale,
        top: 2735 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1256 * scale_offset - 50 / scale,
        top: 2429 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 969 * scale_offset - 50 / scale,
        top: 2480 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 861 * scale_offset - 50 / scale,
        top: 2349 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 903 * scale_offset - 50 / scale,
        top: 2270 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1059 * scale_offset - 50 / scale,
        top: 2170 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 812 * scale_offset - 50 / scale,
        top: 2151 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 742 * scale_offset - 50 / scale,
        top: 1941 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 794 * scale_offset - 50 / scale,
        top: 1820 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1297 * scale_offset - 50 / scale,
        top: 2896 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1255 * scale_offset - 50 / scale,
        top: 1736 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 880 * scale_offset - 50 / scale,
        top: 2628 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 878 * scale_offset - 50 / scale,
        top: 2635 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1309 * scale_offset - 50 / scale,
        top: 3044 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.local_drink,
              size: 10 / (scale*0.7),
              color: Colors.blue,
            ),
          ),
        )
    ),
  ];

  return widgets;
}

List<Widget> showerPositionedList({required double scale, required double scale_offset}) {
  List<Widget> widgets = [
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1579 * scale_offset - 50 / scale,
        top: 2500 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.shower,
              size: 11 / (scale*0.7),
              color: Colors.lightBlueAccent,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1659 * scale_offset - 50 / scale,
        top: 2845 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.shower,
              size: 11 / (scale*0.7),
              color: Colors.lightBlueAccent,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1000 * scale_offset - 50 / scale,
        top: 2439 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.shower,
              size: 11 / (scale*0.7),
              color: Colors.lightBlueAccent,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 822 * scale_offset - 50 / scale,
        top: 2730 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.shower,
              size: 11 / (scale*0.7),
              color: Colors.lightBlueAccent,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1915 * scale_offset - 50 / scale,
        top: 3063 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.shower,
              size: 11 / (scale*0.7),
              color: Colors.lightBlueAccent,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 733 * scale_offset - 50 / scale,
        top: 1848 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.shower,
              size: 11 / (scale*0.7),
              color: Colors.lightBlueAccent,
            ),
          ),
        )
    ),
  ];

  return widgets;
}

List<Widget> storePositionedList({required double scale, required double scale_offset}) {
  List<Widget> widgets = [
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2412 * scale_offset - 50 / scale,
        top: 2747 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.store,
              size: 10 / (scale*0.7),
              color: Colors.purple,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1907 * scale_offset - 50 / scale,
        top: 2942 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.store,
              size: 10 / (scale*0.7),
              color: Colors.purple,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1007 * scale_offset - 50 / scale,
        top: 2756 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.store,
              size: 10 / (scale*0.7),
              color: Colors.purple,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1002 * scale_offset - 50 / scale,
        top: 2354 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.store,
              size: 10 / (scale*0.7),
              color: Colors.purple,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 659 * scale_offset - 50 / scale,
        top: 2719 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.store,
              size: 10 / (scale*0.7),
              color: Colors.purple,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1099 * scale_offset - 50 / scale,
        top: 1713 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.store,
              size: 10 / (scale*0.7),
              color: Colors.purple,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1228 * scale_offset - 50 / scale,
        top: 3178 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.store,
              size: 10 / (scale*0.7),
              color: Colors.purple,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1672 * scale_offset - 50 / scale,
        top: 2808 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.store,
              size: 10 / (scale*0.7),
              color: Colors.purple,
            ),
          ),
        )
    ),
  ];

  return widgets;
}

List<Widget> printerPositionedList({required double scale, required double scale_offset}) {
  List<Widget> widgets = [
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1706 * scale_offset - 50 / scale,
        top: 2717 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1644 * scale_offset - 50 / scale,
        top: 2866 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1941 * scale_offset - 50 / scale,
        top: 2934 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2212 * scale_offset - 50 / scale,
        top: 3039 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2360 * scale_offset - 50 / scale,
        top: 3156 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2407 * scale_offset - 50 / scale,
        top: 2775 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2600 * scale_offset - 50 / scale,
        top: 2691 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1334 * scale_offset - 50 / scale,
        top: 2911 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 986 * scale_offset - 50 / scale,
        top: 2760 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 858 * scale_offset - 50 / scale,
        top: 2586 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 850 * scale_offset - 50 / scale,
        top: 2622 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 950 * scale_offset - 50 / scale,
        top: 2476 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 855 * scale_offset - 50 / scale,
        top: 1987 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 785 * scale_offset - 50 / scale,
        top: 1849 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1139 * scale_offset - 50 / scale,
        top: 1736 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1248 * scale_offset - 50 / scale,
        top: 1773 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1644 * scale_offset - 50 / scale,
        top: 2866 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1290 * scale_offset - 50 / scale,
        top: 3058 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.print,
              size: 10 / (scale*0.7),
              color: Colors.green,
            ),
          ),
        )
    ),
  ];

  return widgets;
}

List<Widget> atmPositionedList({required double scale, required double scale_offset}) {
  List<Widget> widgets = [
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1166 * scale_offset - 50 / scale,
        top: 1751 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.monetization_on,
              size: 10 / (scale*0.7),
              color: Colors.red,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 942 * scale_offset - 50 / scale,
        top: 2418 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.monetization_on,
              size: 10 / (scale*0.7),
              color: Colors.red,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 721 * scale_offset - 50 / scale,
        top: 2611 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.monetization_on,
              size: 10 / (scale*0.7),
              color: Colors.red,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1204 * scale_offset - 50 / scale,
        top: 3175 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.monetization_on,
              size: 10 / (scale*0.7),
              color: Colors.red,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1964 * scale_offset - 50 / scale,
        top: 2941 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.monetization_on,
              size: 10 / (scale*0.7),
              color: Colors.red,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2201 * scale_offset - 50 / scale,
        top: 3035 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.monetization_on,
              size: 10 / (scale*0.7),
              color: Colors.red,
            ),
          ),
        )
    ),
  ];

  return widgets;
}

List<Widget> loungePositionedList({required double scale, required double scale_offset}) {
  List<Widget> widgets = [
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 753 * scale_offset - 50 / scale,
        top: 1877 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.desk,
              size: 10 / (scale*0.7),
              color: Colors.brown,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1221 * scale_offset - 50 / scale,
        top: 1760 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.desk,
              size: 10 / (scale*0.7),
              color: Colors.brown,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 767 * scale_offset - 50 / scale,
        top: 2246 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.desk,
              size: 10 / (scale*0.7),
              color: Colors.brown,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 991 * scale_offset - 50 / scale,
        top: 2691 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.desk,
              size: 10 / (scale*0.7),
              color: Colors.brown,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1184 * scale_offset - 50 / scale,
        top: 2838 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.desk,
              size: 10 / (scale*0.7),
              color: Colors.brown,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 1689 * scale_offset - 50 / scale,
        top: 2712 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.desk,
              size: 10 / (scale*0.7),
              color: Colors.brown,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2015 * scale_offset - 50 / scale,
        top: 2944 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.desk,
              size: 10 / (scale*0.7),
              color: Colors.brown,
            ),
          ),
        )
    ),
    Positioned(
        width: 100 / scale,
        height: 40 / scale,
        left: 2241 * scale_offset - 50 / scale,
        top: 2923 * scale_offset - 20 / scale,
        child: Container(
          child: Align(
            alignment: Alignment.center,
            child: Icon(
              Icons.desk,
              size: 10 / (scale*0.7),
              color: Colors.brown,
            ),
          ),
        )
    ),
  ];

  return widgets;
}
